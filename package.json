{
	"name": "robelest",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev:convex": "convex dev",
		"build": "bun run site/build.ts",
		"sync": "bun run scripts/sync.ts",
		"sync:prod": "bun run --env-file=.env.production scripts/sync.ts",
		"deploy": "bun run build && bunx @convex-dev/self-hosting upload --dist ./dist --component staticHosting --prod",
		"deploy:dev": "bun run build && bunx @convex-dev/self-hosting upload --dist ./dist --component staticHosting",
		"deploy:convex": "convex deploy"
	},
	"devDependencies": {
		"bun-types": "^1.3.9",
		"solid-jsx-oxc": "0.1.0-alpha.15",
		"tailwindcss": "^4.1.13",
		"typescript": "^5.9.2"
	},
	"dependencies": {
		"@convex-dev/self-hosting": "^0.1.0",
		"convex": "^1.30.0",
		"convex-solidjs": "^0.0.3",
		"gray-matter": "^4.0.3",
		"hono": "^4.11.9",
		"katex": "^0.16.27",
		"marked": "^17.0.1",
		"p-limit": "^7.2.0",
		"solid-js": "^1.9.11"
	}
}
